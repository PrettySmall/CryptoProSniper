import assert from 'assert';
import dotenv from 'dotenv'
import * as utils from './utils'
import * as uniconst from './uniconst'
import * as afx from './global'
import { DelayDetector } from "./delay_detector"
dotenv.config()

export const buyToken = async (database: any, session: any, tokenAddress: string, buyAmount: number) => {

}


export const sellToken = async (database: any, session: any, tokenAddress: string, sellPercent: number) => {

   
}

export const transferETH = async (database: any, session: any, destWallet: string, amount: number) => {

}
